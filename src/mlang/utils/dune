(include_subdirs no)

(library
  (name utils)
  (package mlang)
  (modules strSet setExt pos pp mapExt strMap setSetExt intSet
           errors intMap config cli dgfip_options dgfip_m file)
  (libraries cmdliner ANSITerminal dune-build-info threads))
;
; (library
;   (name cli)
;   (package mlang)
;   (modules cli)
;   (virtual_modules cli))
