(include_subdirs no)

(library
 (name m_ir)
 (flags
  (:standard -open Utils -open Utils_common -open Platform))
 (modules
  com
  dbg_info
  format_mir
  mir_interpreter
  mir
  mir_number
  mir_roundops)
 (package mlang)
 (preprocess
  (staged_pps ppx_deriving.show ppx_deriving_yojson))
 (libraries utils ocamlgraph re zarith gmp))
